<p id="notice"><%= notice %></p>

<p>
      <strong>Description:</strong>
      <%= @meow.description %>
</p>

<p>
      <strong>User:</strong>
      <%= @meow.user.email %>
</p>

<%= link_to @follow_status, toggle_follow_meow_path(user_id: @meow.user_id) %>
<%= link_to @like_status, toggle_like_meow_path(user_id: @meow) %>
<p>
     Likes:<%= @meow.likers(User).count %>
</p>
<%= link_to 'Edit', edit_meow_path(@meow) %> |
<%= link_to 'Back', meows_path %>
